# Acme — Sitemap-Derived URL Inventory

> **Generated:** YYYY-MM-DD
> **Source:** `https://acme.example/sitemap.xml`
> **Method:** Fetched sitemap, extracted all `<loc>` entries, grouped by URL pattern, sampled response codes.

This is the **canonical URL inventory** for the migration. Every page we need to make a migrate/drop/rebuild decision on either appears here or is gated content from outside the sitemap (`site-discovery.md` §3).

Update this file when re-running the crawl. Keep the raw counts current — `migration-analysis.md` references them.

---

## Summary

- **Total URLs in sitemap:** ~1,200
- **Active (200 OK):** ~830
- **Redirected (3xx):** ~210
- **Errors (4xx/5xx):** ~160
- **Substantive migration surface (after stale filter):** ~800

## By URL pattern

| Pattern | Count | Status mix | Notes |
|---------|-------|------------|-------|
| `/products/{slug}` | 180 | All 200 | Structured content type |
| `/case-studies/{slug}` | 60 | All 200 | Structured content type |
| `/blog/{y}/{m}/{slug}` | 412 | 380 ok, 32 redirected | Editorial backlog |
| `/about/*` | 12 | All 200 | Static editorial |
| `/legacy/*` | 87 | All 301 → /products | Redirect-only — drop |
| `/node/N` orphans | 95 | 403 | Hidden content — needs DB access |
| Other (`/tag/`, `/author/`, etc.) | 354 | Mixed | Listing/index pages — auto-regenerate on new platform |

## Stale-content filter

Of the ~830 active URLs:

- ~150 are duplicate listing pages (paginated `/blog/page/N`) → regenerated by the new platform, don't migrate.
- ~30 are legacy "promo" pages from old campaigns → drop unless client wants to keep.
- ~5 are placeholder pages from a previous CMS migration → drop.

Net **migrate surface ≈ 645 pages** before client editorial pass.

## What's missing from the sitemap

- Gated member-area content (~unknown, see `site-discovery.md` §3).
- Asset libraries — PDF spec sheets, image galleries — not crawled.
- Hidden orphan content (`/node/N` 403s above) — likely real but unreachable.

## Raw data

Per-pattern counts above derived from `scripts/inventory-acme.txt` (not committed — regenerate when needed).

```
# Regenerate:
curl -s https://acme.example/sitemap.xml | grep -oP '(?<=<loc>)[^<]+' | sort -u > inventory.txt
```
